/*
package com.HotelShare.entities.Currency;

public class CurrencyAdapter {
    public static CurrencyDTO toCurrencyDTO(Currency currency) {
        return currency != null ? CurrencyDTO.builder()
                .id(currency.getId())
                .nameCurrency(currency.getNameCurrency())
                .createdDate(currency.getCreatedDate())
                .updatedDate(currency.getUpdatedDate())
        .build() : null;
    }

    public static Currency toCurrency(CurrencyDTO currencyDTO) {
        return currencyDTO != null ? Currency.builder()
                .id(currencyDTO.getId())
                .nameCurrency(currencyDTO.getNameCurrency())
                .createdDate(currencyDTO.getCreatedDate())
                .updatedDate(currencyDTO.getUpdatedDate())
        .build() : null;
    }
}
*/
